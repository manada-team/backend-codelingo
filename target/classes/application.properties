# Servidor
#server.port=8080

# Base de datos PostgreSQL
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/codelingo}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
codelingo.jwt.secret=${JWT_SECRET:Y29kZWxpbmdvLXNlY3JldC1rZXktcGFyYS1kZXNhcnJvbGxvLWxvY2FsLTIwMjQ=}
codelingo.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# Email (Gmail SMTP)
# IMPORTANTE: usar una contrasena de aplicacion de Google, NO la contrasena real
# Generarla en: https://myaccount.google.com/apppasswords
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:codelingo.ua@gmail.com}
spring.mail.password=${MAIL_PASSWORD:inshwklekpucjbwk}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
#codelingo.frontend.url=${FRONTEND_URL:https://codelingo-tau.vercel.app}
# URL del frontend (para los links en los emails)
codelingo.frontend.url=${FRONTEND_URL:http://localhost:3000}
+
# Nombre de la app
spring.application.name=backend-codelingo

logging.level.org.springframework.security=DEBUG
logging.level.com.codelingo=DEBUG

server.address=0.0.0.0
server.port=${PORT:8080}